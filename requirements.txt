# aemo-mdff-reader has no required runtime dependencies.
# (The Python import name is ``aemo_mdff_reader``.)
#
# Optional features can be enabled via extras:
#   pip install aemo-mdff-reader[pandas]   # to_dataframe(), parquet output
#   pip install aemo-mdff-reader[parquet]  # parquet output (pandas + pyarrow)
#   pip install aemo-mdff-reader[mysql]    # SQL persistence (Storer)
#   pip install aemo-mdff-reader[dev]      # test suite, linting
#
# This file is kept for ``pip install -r requirements.txt`` users; it
# installs the package in editable mode along with the dev extras.
-e .[dev]
